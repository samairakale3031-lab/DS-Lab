#include <stdio.h>

int main() {
    int n, key, i, found = 0;

    // Prompt user for size of array
    scanf("%d", &n);

    int arr[n];

    // Read array elements
    for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Read search element
    scanf("%d", &key);

    // Perform linear search
    for (i = 0; i < n; i++) {
        if (arr[i] == key) {
            printf("found at position %d\n", i);
            found = 1;
            break;
        }
    }

    // If not found
    if (!found) {
        printf("%d not found\n", key);
    }

    return 0;
}
